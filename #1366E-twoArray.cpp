#include<iostream>
#define FASTIO ios_base::sync_stdio(0);cin.tie(0);cout.tie(0);
using namespace std;
int main(){
	FASTIO;
	 int n,m;
	 cin>>n>>m;
	 int a[n],b[m];
	    for(int i=0;i<n;i++)cin>>a[i];
		for(int j=0;j<m;j++)cin>>b[j];
		
     long long int s=1;
	 long long count=0;
	 
	   if(m>n)cout<<"0";
	   
			 
		 
			 
	 